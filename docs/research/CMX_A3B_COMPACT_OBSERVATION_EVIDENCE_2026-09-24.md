# CMX A3b — executable compact native observation evidence

## Outcome
On Terminal #740's existing branch, A3's real one-shot research executable now offers `--view compact`. It turns the actual native computation into a small source-linked observation without an LLM, browser, network, second indicator algorithm or new state owner. Full mode remains the default. This is BUILT_NOT_PROVEN for production Brain/Fabric enrollment; the overall customer path remains PARTIAL.

Parent operation MMX-AI-TERMINAL-ENV-BUILD-20260924-SOL-001. Chairman review waiver #7151 comment5826034970 remains in force. Protected procedure Mastermind@a21370ef17f71beb51b798df218c4b5e4b01d3e0; prior semantic head5d9d1397d7d547cdfe655e9ec0a1670efc8dafa6. Fresh protected Terminal94403c1dd6693c41ae2100a9deafa2ce968ffa1b had no movement in the checked native/A3/AGENTS/package dependency paths. Exact open-PR census found no competing A3/compact consumer; #607/#675's native host remains untouched.

## What becomes usable
The compact response contains original data/settings/result fingerprints, actual executable identity, whole full-snapshot content hash, native module switches/locks, and selected native values with JSON pointers. Up to six series expose two newest actual samples, up to eight events retain native confirmation timing, up to four right-extended lines/zones retain coordinates, and up to four table rows retain original text/columns/footnotes. The response stays within12288 actual UTF-8 JSON bytes plus its newline. Whole-fact omission and invalidity counts are explicit; selection is presentation, never opportunity ranking.

A missing latest series value remains null; an old last sample exposes its age rather than masquerading as current. Native y values are not assumed to be dollar prices; native event strengths are not probabilities. A table's resampling footnote cannot be cut away to save space. Right-edge geometry is not a future timestamp or proof of knowability. Unknown warmup/module health, caller-asserted finality/revision, unavailable knowledge time, renderer limits, and no predictive/signal authority survive unchanged from A3. Empty/locked/disabled outputs never turn into no-setup judgments. Native text is data, not instructions.

## Executed tests
- Before implementation, all19 initial compact-view tests failed for the missing component.
- Before CLI integration, the old executable failed5 of16 cases: it refused the new view flags; the original7 tests and invalid-flag controls retained their expected behavior.
- Final compact-projection tests:26 passed, including all-five-suite exact-value/source-pointer checks and controlled delayed events/nulls/oscillator/Unicode/footnote cases.
- Final actual bundled-executable tests:16 passed, including unchanged full default, explicit full, compact/full hash equality, free-tier default, flag-order equivalence, malformed/duplicate flags, request-authority refusal, inherited upstream refusal and source-size reduction.
- Final whole unit suite:386 files,6227 passed,4 existing TODOs,exit0. Native test process79641 completed; its final exit JSON and logs were read from the original target. A later process-status read found no running original process, not an orphaned test or permission to rerun it.
- Both Terminal and Node-only source graph typechecks exited0. The CLI test also typechecks the Node graph before building. No dependency/lock changes.
- Existing A1/A1b browser evidence remains at its prior source; no new browser or production-model claim is made for this Node-only extension.

## Actual recorded-data exercise
Twenty executable invocations compared full/compact output on five suites for each of NVDA and AAPL, using explicit trailing420-row windows from the repository's recorded files ending2026-06-26. Every pair exited0 with empty stderr. Full output ranged44357–107686 bytes; compact output5511–6880 bytes, a measured87.57–94.00% byte reduction. These are byte results, not measured token cost, task accuracy, model latency, or trading improvement. Numerical fingerprints match the full native result, and316 exact source/value/timing references were checked across the ten pairs.

Two more real executable invocations enabled the native RSI MTF module. The compact response retained all three displayed rows and the real footnote: rows are resampled from the loaded bars, not independent timeframe feeds. That is a source observation, not qualified intraday data. Recorded provider provenance and point-in-time availability were not independently revalidated.

Runtime: Nodev26.5.0; esbuild0.21.5; targetnode20 is compilation only. Tested executable SHA25675383cb8da51e9b100705505a0479c52f8614ea34703cfaa2e51399bb417443c. Only external imports node:crypto,node:fs,node:url. Original stdout snapshots remain in ignored local diagnostics; committed proof JSON preserves data/code/output hashes, sizes, exact windows and per-pair results. No new artifact registry/store.

## Delivery and remaining dependency
Source: ingest/native_suite_observation.ts plus the existing CLI; tests in terminal/lib/__tests__; use contract in ingest/NATIVE_SUITE_SNAPSHOT.md. Spec/plan: docs/superpowers/{specs,plans}/2026-09-24-cmx-a3b-compact-native-observation.md.

The backend command-format defect in #740 comment5814929845 remains open, as do ACK forwarding and exact-origin/viewport closure. No prior denied Macro inspection was retried/rephrased/delegated, and no second proxy patched missing metadata. No worker, inference provider, new credential, production/customer/trading operation or autonomous roam was started. Source repair and real customer-path proof remain required before release. Keep #740 draft/held until those gates are actually satisfied; independent review is not a gate under the Chairman waiver.

## Next integration boundary
Do not keep adding unrelated features to hide the denied customer-path repair. The compact projection is now a usable internal consumer. The next product work must establish a permitted backend-source recovery and close the real producer/consumer contract, or independently qualify how the existing native guides map these observed values into technical skills without granting trade authority. Both still need their actual input/owner gates. Current view selection is not proof that any model interprets the evidence well.
