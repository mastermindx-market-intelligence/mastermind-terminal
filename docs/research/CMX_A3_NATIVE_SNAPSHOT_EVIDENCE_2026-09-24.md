# CMX A3 — executable native research snapshot evidence

## Outcome and current state

On the existing Terminal #740 carrier, an internal research process can now supply daily bars and native settings, run the actual Terminal suite kernels without a browser, and receive a reproducible detached JSON observation or a typed refusal. This is a working local machine consumer, not merely a schema. It is BUILT_NOT_PROVEN for production integration. Parent program MMX-AI-TERMINAL-ENV-BUILD-20260924-SOL-001 remains incomplete.

Independent review remains REVIEW_WAIVED_BY_CHAIRMAN (Macro #7151 comment5826034970). No reviewer is launched or claimed. The denied Macro source inspection is not retried; its real backend command defect remains the first customer-release dependency. This independent A3 work does not conceal or repair that defect.

## Existing owners consumed

- A1 readNativeSuiteParams and existing suite metadata/defaults.
- ensureSuiteRuntime and computeSuite; no reimplemented formula, force-enabled module collection, or parallel indicator registry.
- resolveSuiteColors for the same deterministic non-browser fallback palette.
- suiteEventTiming and validSuiteBarClock for the existing confirmation-index meaning.
- A one-shot Node executable; no scheduler, jobs store, provider/credential loader, data fetch, alerts write, or new MCP server.

Protected procedure: Mastermind@622d128d64a79c3e4dd45f748b8a8db7ec2e2741. Parent Terminal source78a421373cfa04746ef4e60e1ac1de91ca87052e. Refreshed protected Terminal94403c1dd6693c41ae2100a9deafa2ce968ffa1b. Bounded comparison found no movement of candidate paths, native metadata/runtime/host/event-clock or dependency-lock files from the original base. Open-PR census retained #607/#675 host and #647 event-owner work; those files are not edited.

## Executed evidence

- Missing research-consumer RED:30 tests failed. Missing executable RED:7 tests failed.
- Native snapshot tests **40 passed**, including actual manual/adapter output equality for Structure Core and RSI Ultimate, disabled behavior for all five suites, native tier enforcement, changed-history fingerprints, cache detachment, input refusals, confirmation mapping, output byte refusal and thrown-host failure.
- Bundled CLI tests7 passed: actual fresh Node processes, deterministic repeat, executable fingerprint, free default, no request-tier injection, malformed input, operator-argument refusal and real UTF8 input cap.
- Final combined focused tests **88 passed**, retaining A1 native control/capabilities tests.
- Both Terminal and the separate Node-only source graph typechecked successfully. The CLI test now invokes semantic typechecking before its build so hosted tests cover the Node graph too.
- Full final unit result: **6,192 passed in385 files; four existing TODOs; exit0** (native process1446). Earlier process80278 passed6,187 before the five final boundary cases were added. Existing negative-case tests emit expected diagnostic stderr; no assertion failures remained.

Five additional real executable invocations on420 synthetic daily rows exercised all five native suites at defaults. Ten invocations used explicit trailing420-row windows from pinned repository NVDA/AAPL files. All15 produced observations with exit0 and no stderr. The recorded files assert polygon/real_ohlc, but their provider provenance was NOT independently revalidated; they end2026-06-26 and are not current-market data. Native events on these samples all resolved through the existing timing helper; this is not proof of wall-clock availability or absence of repainting.

The individual code/input/output hashes, sizes and counts are in `docs/research/cmx_a3_native_snapshot/executable-proof.json` and `recorded-input-proof.json`. Build: esbuild0.21.5, targetnode20; actual local runtimeNodev26.5.0. The bundle's only external imports were node:crypto, node:fs and node:url. Compilation target is not a claim of having executed a Node20 canary.

## Debugging and evidence hygiene

Initial parity matched both actual kernels, but the first simplistic SR fixture produced zero levels. Reused A1's known420-row oscillating fixture instead of changing the algorithm or dropping the nonempty assertion. A locale-dependent key-order issue was reproduced RED and changed to code-unit ordering for portable fingerprints.

The first full suite found an older own-task log under terminal/test-results containing current_password; the existing all-extension account boundary test correctly detected it. That log was moved outside the scanned Terminal tree. No account code/test guard was weakened. The CLI test environment also needed explicit NODE_ENV for Next's ProcessEnv type, and the standalone typecheck needed the already-installed Terminal Node type root. No dependency was installed or changed.

## Honest capability boundary

Request: one suite, explicit settings, strictly ordered dated OHLCV, maximum2000 daily bars; stdin maximum1MiB. JSON cannot claim host tier or code identity. Trusted local operator tier remains separate and defaults to free. Native defaults merge without silently turning disabled modules on.

Output: exact materialized renderer bundle, settings and data/result fingerprints, native events and source-bar confirmation projection. Nonfinite visual values become disclosed nulls, never zero. Oversized output refuses rather than truncates. This is a machine artifact, not an invitation to flood model context with every primitive.

The existing renderer can cap primitives and suppress individual module exceptions. Every observation explicitly preserves UNKNOWN module health/warmup, caller-asserted finality/revision, absent knowledge-time proof, renderer-bundle rather than complete raw event-ledger scope, native strength rather than probability, and no predictive or trade authority. An empty or locked bundle cannot be treated as no setup.

No chart-context, backend, BrainWidget, native kernel, indicator host, alert, account, provider or replay owner was edited. No visible UI design was introduced. No inference, performance backtest, production deployment, customer mutation, automatic roaming or installed fabric capability is claimed.

## Use / continuation

The actual build/run instructions are in `ingest/NATIVE_SUITE_SNAPSHOT.md`; frozen scope/plan are in docs/superpowers/specs and plans, named2026-09-24-cmx-a3-native-research-snapshot.md. Core sources are ingest/native_suite_snapshot.ts, ingest/native_suite_snapshot_cli.ts and its dedicated tsconfig.

Next: repair the producer-to-client wire through a permitted source path, not a weaker client validator; retain A1/A1b/A3 and the waived review condition. Then connect exact target/context/receipts and compact instrument observations to Brain through incumbents. A3 must not grow a second scientific evaluation, worker queue, source registry or trading-policy layer.

## Final boundary repair

Direct scrutiny reproduced inherited suite names (`__proto__`, `constructor`, `toString`) reaching an inherited metadata property rather than producing a typed refusal. The new adapter now checks membership through the existing canonical SUITE_ORDER before invoking metadata validation. No registry, native host, or A1 behavior was rewritten. Two control-character revision cases also pass. The executable proof was refreshed after this guard change: all15 observations retain exactly the same numerical fingerprints; code and full-output hashes were re-bound to the new executable.

Final verification run1446 completed with focused88/full6,192 passing tests, four existing TODOs and exit0. Final source TypeScript check6496 also exited0. The source/test digests in source-evidence.json bind this exact content; the refreshed executable digest is41f40171c6c2610b9a25facf8793b8140f620637a2e0de535d9691290fe54f6e.
