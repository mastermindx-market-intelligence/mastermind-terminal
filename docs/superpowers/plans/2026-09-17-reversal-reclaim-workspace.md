# Reversal & Reclaim — a cross-suite chart workspace

**Goal:** A user can apply one coherent structural/momentum/price-confirmation study arrangement, understand its role and limitations, inspect its real modules, and undo the change without rebuilding a chart manually.
**Authority:** Chairman-approved charting upgrade, full Sol leadership. Operation CHART-RECLAIM-WORKSPACE-20260917-SOL-001, source 75c22083249e7a1529be3d6baf819b9ad5ea509f, Skillpack Mastermind@8b231e8267f09cfb002ed3e87bec14906dce1720.
**Isolation:** Fresh owned Terminal worktree. This lane never edits A2a #604 position rendering, its blocked refinement, or A1 #602 alert sources. #600 is now merged; its deployment preflight must be loaded before release.

## Design
Extend the existing suites/presets.ts owner with one cross-suite workflow recipe, not a new indicator/scientific registry. It selects Structure Smart S/R, Swing Failure and Auto Patterns; Trend Engine and Mastermind Candles; RSI Engine, Signals and regular Divergence; and existing Gap Zones. Fixed display overrides quiet targets/labels, expose invalid swing failures, and disable historical auto-optimization. Calculation inputs (RSI length, ATR sensitivity, level sensitivity) remain the user's settings. The card explicitly discloses the display recipe/autoOpt change.

Applying replaces built-in/suite studies; noncatalogue extensions, comparisons, Pine scripts and drawings are not deleted. Other stored module parameters and unknown forward-compatible fields remain. A one-step in-session undo uses React state only, restoring existing chart owners; it is available only while the exact applied state is unchanged. No new persistent workspace or control plane. Entitlements and anonymous study limits remain existing gates.

The current All Indicators landing retains its catalogue, with a compact `Start with a workflow` action. Existing Systems & Presets renders a high-visibility Reversal & Reclaim card above legacy suite recipes. Its three steps are Location → Momentum → Price confirmation, with explicit chart-timeframe-only and forming-bar limitations. Guide links use the existing Guide Center. Applied state is derived from actual active studies and settings, never a fake flag. Existing VisualIntelligencePanel remains the current readout owner; no conflicting signal score or occurrence lifecycle is created.

## Acceptance
- Test-first pure recipe application: no input mutation, no duplicate registry, valid module IDs, math settings/unknown fields preserved, quiet display/autoOpt explicit, hidden selected modules restored, unknown recipe no-op, exact state comparison for undo.
- Actual UI: apply, inspect rendered modules and persistence, correct entitlement, undo exact original state, abandon undo after later custom edits, real responsive browser proof in dark EN/ZH. Terminal is explicitly dark-only; no fake light-mode proof.
- Full typecheck/unit, relevant existing guide/preset browser regressions, source review, native required checks and lawful deployment proof. No assertion that this recipe validates a trading edge.

## Parent remaining work
This unlocks coherent composition, not the full synthesized setup state. Linked price/pane divergence and owner-native forming/confirmed setup projection still require their exact interfaces. A2a original/remaining R remains held for a blocked visual repair; do not reproduce its denied edit in this lane. Scientific scanner/options adoption remains governed by TOI/Radar/Options/Evaluation owners and their data/proof gates.

## Material checkpoint
Recipe RED-to-GREEN completed; full unit 5,517 pass + 4 existing TODOs / 333 files. Six real chart/undo browser cases passed (three viewports × EN/ZH); three additional shared gate cases passed; real Guide Center return passed. Twelve captures preserved. Source implementation and screenshots remain a candidate until native checks/review and deployed browser proof. See docs/research/REVERSAL_RECLAIM_WORKSPACE_PROOF_2026-09-17.md.


Independent review P2 was consumed and reproduced on the real UI; undo now expires permanently at the first committed custom edit. Native plain-language failure was reproduced and repaired with scoped bilingual pair lookup, leaving central i18n/evidence-locked files byte-identical. Final-source full unit 5,517 pass / 4 TODOs in 333 files; typecheck and added-line copy guard pass. Final browser matrix 10 pass / 8 disclosed shared-gate duplicates skipped, twelve refreshed real captures. Review successor and native CI remain next; source stays on #606.

Keyboard review P2 reproduced and repaired through the existing dialog ref/category transition. Final 11-pass/10-disclosed-skip three-viewport matrix, full unit 5,517 pass/4 TODOs and typecheck pass. The subsequent optional keyboard apply/undo probe was platform-denied before any effect and is frozen; do not claim that additional probe was executed. Twelve exact-source captures refreshed.
