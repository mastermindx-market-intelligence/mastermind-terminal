# CMX A3 — native renderer research snapshot

## Acceptance / scope
One bounded trusted local research invocation supplies explicit daily OHLCV and native suite settings, executes the existing Terminal runtime, and obtains a reproducible JSON observation or a typed refusal. No browser is needed. Same input/settings/tier/colors/language must match the actual computeSuite bundle; no duplicate indicator mathematics.

Parent: Macro #7151; Terminal #740; operation MMX-AI-TERMINAL-ENV-BUILD-20260924-SOL-001. Current Chairman delegation includes research environment implementation; independent review is waived by comment 5826034970. Protected procedure Mastermind@622d128d64a79c3e4dd45f748b8a8db7ec2e2741. A1/A1b remain intact at 78a421373cfa04746ef4e60e1ac1de91ca87052e.

## Placement and rationale
The denied Macro inspection is not retried or delegated. This independent stage implements the already-approved same-kernel research consumer on new Terminal-owned paths, on the existing locked carrier. No eligible worker is currently running; direct bounded implementation has lower total overhead than repeating failed placement. Preserve #597, #606, #607, #647, #654 and #675; no edits to their host/temporal/transport paths.

Alternatives: importing arbitrary scripts is premature; rendering every research chart is unnecessary; a BFF command-normalization workaround would alter the existing pass-through/run contract without the required backend source proof. Retain the real backend defect as a release block instead.

## Contract
- One request: schema chart.native_snapshot_request.v1, suite, symbol, timeframe D, data_revision, bar_state declared_closed, params, bars. Reject unknown properties; request bodies cannot set host tier or code version.
- Exact dated daily OHLCV: strict ISO calendar dates, ascending unique dates; finite positive O/H/L/C, consistent high/low geometry, finite nonnegative volume. Between 1 and 2000 bars. No coercion, sorting, repair, resampling, live fetch, or inferred intraday support.
- The closed-bar and revision fields are CALLER ASSERTIONS, not trusted knowledge-time or market-finality proof. Output says so. No replay/economic qualification claim.
- Host context supplies tier and a 64-hex code digest; unknown/absent context refuses. CLI defaults to free, permits an explicit trusted operator --tier, and hashes its executable bundle. It reads no environment secrets, account, data-path, URL, provider, or arbitrary script from the request.
- Validate params through existing readNativeSuiteParams. Merge canonical defaults without force-enabling modules. Use ensureSuiteRuntime, computeSuite, resolveSuiteColors unchanged.
- Canonical JSON normalizes Maps, typed arrays and key order; nonfinite output numbers are null with a disclosed count, never zero. Return the detached exact render bundle, supplied and effective settings, input/settings/output SHA256, source-bar timing through existing suiteEventTiming, plus raw events.
- Native events retain strength without translating it to probability. Confirmation-bar identity is not wall-clock arrival. Invalid timing is explicit, never silently immediate.
- computeSuite already caps geometry and suppresses individual module exceptions. The observation explicitly states renderer-bundle scope, unknown module health/warmup, and no complete raw event-history claim. Empty/locked output is not a no-setup judgment. Do not change the host or install a parallel evaluator to hide this limitation.
- CLI stdin is capped at 1 MiB; one JSON request and one JSON response, 256 KiB maximum. Oversize output refuses rather than silently truncating. Kernel warnings go to stderr. No persistent job, scheduler, watcher, alert, workspace or customer state.

## Proof
Tests exercise real suite runtimes and compare exact materialized render outputs. Include input rejects, context separation, changed-history fingerprints, settings order stability, disabled/locked modules, no mutation of shared cached results, genuine Node executable runs, bad JSON, byte limits and unsupported timeframe. Existing full unit and typecheck remain gates. The runner is local research capability, not production Brain integration, unrestricted Pine support, installed fabric tool, predictive alpha, or a live customer feature.
