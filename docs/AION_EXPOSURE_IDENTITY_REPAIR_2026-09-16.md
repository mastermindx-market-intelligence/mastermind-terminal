# Exposure desk selected-ticker repair — Terminal #591

## Mission and authority
A person changing the Exposure ticker must never receive the previous ticker's state or levels. Preserve useful same-root matrix data and honest missing levels. This bounded repair advances the Chairman's Aion-informed upgrade; it does not replace the broader forecast/options roadmap.
Protected Sol Skillpack: Mastermind `f590c068880dbb848bda90b80b73dbcb6688d6fc` (compatible 1.0.1/bootstrap 1); COLD_START, ACTIVE_EXECUTION, WEB_CEO_DELEGATION, RECONCILE_STATE and CLOSEOUT loaded at that pin.
Terminal base: `702d81bb35f2c900a5aa1215437bf968aa9e6d93`; source/issue #591 and producer-trace correction comment 5691716246 govern the scope. No Executive Job or external worker is claimed.
Attended direct reason: NO_ELIGIBLE_PRE_EFFECT_WORKER and CRITICAL_PATH_SHORTCUT. Current tool discovery exposes no approved Executive submit app; the exact defect is frozen and no competing open options PR or assigned #591 worker was found. Independent review remains separate.
Source custody: Terminal's prescribed `.claude/worktrees/` and `claude/` branch; the installed mmx-workspace launcher is pinned to the separate Mastermind repository and has no repository selector. No shared checkout files or branch are modified.

## Contract
- Reuse the existing GEX-state request, polling cadence, cache and matrix renderer.
- Accept only the declared state schema and selected root, normalizing the root consistently.
- Fence reversed/overlapping responses, ticker changes and unmount with the current component's request generation.
- Never render an old-root state during the render before the ticker effect clears it.
- Invalid/absent state becomes unavailable; valid matrix cells and finite positive same-root fallback levels remain usable.
- The current Macro matrix producer uses the canonical raw-chain spot-grid flip. Correct the stale Terminal comment; do not remove that legitimate fallback or reconstruct gamma here.
- Preserve source observation timestamps. No age refresh, new forecast, Greek formula, level-growth interpretation, signal, score, data store or permission change.
- Existing design, tokens, EN/ZH copy and responsive layout are unchanged. No new visual design is commissioned.

## Test-first sequence and acceptance
1. Add a selected-root envelope/price test and mounted GexDeskView regression with deferred flow responses; observe their pre-fix failure.
2. Add the narrow reader guard, request fence and render-time root filter; keep current-method matrix fallback and normalize only invalid price fields to null.
3. Prove old-root responses, repeated same-root requests, missing/wrong-schema data, failure-to-unavailable and legitimate fallback through the actual component.
4. Run relevant and full existing Vitest/typecheck gates, then cold responsive browser proof at desktop/tablet/mobile with dark/light and EN/ZH coverage. Synthetic fixtures are not production data proof.
5. Commit and push one scoped PR; consume current-head CI and independent review before lawful merge. Production deployment requires the existing current deployment gates and real served-path proof.
6. Stop at a genuine gate with exact evidence and preserved source; never claim that tests, a PR or a synthetic reproduction are a deployed fix.

## Continuation
Liquidity #124 stays separate at `23f97360` with failed hosted CI and unplaced review. Do not change Executive filesystem security gates to make its native tests green. Record actual implementation/proof results below before closeout.

## Implemented candidate and observed verification
The existing desk now admits state by exact schema/normalized selected root, rejects invalid plotted prices, fences pending state completions with a component request generation, invalidates the generation on cleanup, and filters every rendered state consumer by the selected root before passive cleanup. The existing matrix merger independently refuses an explicitly different state root and preserves finite same-root fallback values. No layout, pricing formula, source date, collector or cadence changed.

The 22 new data-contract regressions had a discriminating baseline: 20 failed and 2 valid-fallback cases passed before implementation; all 22 pass after it. The focused existing matrix/ladder/session suites plus new tests passed 136 tests. Full existing Vitest: **327 files passed; 5,434 tests passed; 4 pre-existing todo**. TypeScript `tsc --noEmit` passed.

The cold existing responsive Exposure tests ran with CI=1, a separately checked port 32171, one worker, zero retries and no reused server. **8 passed / 1 pre-existing skip** across 1440x900 desktop, 820x1180 tablet and 390x844 mobile. The skip is the existing archived-matrix mobile case, not a new exclusion. Desktop/mobile captures are EN; tablet matrix is ZH. These captures are dark-theme fixture evidence. Light-theme acceptance is not established by this run.

The dedicated mounted deferred-response regression file was blocked at tool execution before creation. It was not retried through another tool, path or actor. Therefore the specific reversed-response customer journey is source-repaired but lacks its intended mounted-component regression proof. The passing existing browser tests do not substitute for that missing proof. This is a platform-limited test lane, not a claim that the implementation is production-proven.

## Remaining release gates
- Recover an approved way to verify the exact mounted race and render-time identity boundary, without retrying a denied operation through another carrier.
- Independent review of the exact source head; builder review is not independence.
- Current-head hosted CI, remaining required responsive/light evidence, lawful merge/deploy and actual production verification.
- Keep the PR draft while these gates remain. Do not call the 8 existing browser passes a full browser-suite pass or call fixture dates fresh market evidence.

## Next product boundary retained
The screenshots confirm that basic matrix/ladder capabilities already exist. The next product improvement is to give matrix analysis enough visible working area and then add defensible observation-time change interpretation through the existing owners, not create another matrix or gamma estimator. This is a proposal, outside the narrow #591 code repair.
