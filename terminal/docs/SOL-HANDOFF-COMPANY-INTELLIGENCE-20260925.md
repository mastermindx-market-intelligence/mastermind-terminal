# SOL HANDOFF — Company Intelligence implementation and production acceptance

Created: 2026-09-25T03:14:18Z
Chairman mission: finish replacing Terminal's legacy Company Intelligence experience with the reviewed Paper design, extend it into a genuinely useful research workflow, release through the protected GitHub chain, deploy through the incumbent exact-target Terminal owner, and prove the authenticated real production path.

## Finalization

- `FINALIZATION_CLASSIFICATION: CHECKPOINTED_CONTINUATION`
- `MISSION_COMPLETE: false`
- Intended resume surface: a fresh Sol implementation/release session with Studio Direct and GitHub access, starting from this file and Terminal issue #679.
- This handoff transfers context, not source custody, an active PR lease, a runtime Attempt, deployment permission, or an automatic wake.
- No watcher or child worker was started by the checkpointing session.

## Current protected procedure and source

- Mastermind Skillpack pin: `mastermindx-market-intelligence/Mastermind@605cd056c3463c992d85ba76dbcc90fbb758da75`
- Skillpack: `mastermind.sol_skillpack.v1` / `1.0.1` / bootstrap major `1`
- Protected Terminal master at checkpoint: `mastermindx-market-intelligence/mastermind-terminal@9aca42ee0047651856c8d7b632d8737b0dffd569`
- Current master subject: `feat(analysis): rebuild Sources & Method from Paper (#730)`
- Parent product issue / durable narrative owner: Terminal issue `#679`
- Research Workspace compatibility issue: Terminal issue `#699`
- Paper operation: `terminal-company-intelligence-redesign-20260920-sol-001`
- Canonical Paper file/page: `MASTERMIND PAGES` / `01M2WGNCX9475G79JRKJTCM08P` / `p-4-1 — Company Intelligence · 2026-09-20`

## Capability ledger

### Accepted on protected master — DO NOT REDO

- Brief + Evidence: PR `#725`, merge `1d2ac1e64a21c957b229e2e2567fcc248d557a64`.
- Results & Outlook: PR `#727`, merge `1782ba073a04853c406a1c0909dd64fede28dec0`.
- Lens scroll-stability: PR `#743`; never restore `scrollIntoView` behavior when switching Intelligence lenses.
- Call + Q&A: PR `#728`, merge `febda456bf584a428aa4ed13ff596869116e0283`.
- Sources & Method: PR `#730`, merge/current master `9aca42ee0047651856c8d7b632d8737b0dffd569`.

These accepted slices preserve one event/source/transcript/evidence plane. Call + Q&A uses verified exchange/speaker/span structure, keeps the incumbent transcript reader/search owners, and explicitly renders the governed topic taxonomy as unavailable rather than inventing Paper's illustrative topics.

### Active release edge — Institutional Ownership

- PR: `#734 — feat(analysis): promote Institutional ownership workspace`
- Exact candidate head: `b2b511144d609acf807ffc8bdcb35b61ea5b613a`
- Exact base at checkpoint: protected master `9aca42ee0047651856c8d7b632d8737b0dffd569`
- State at 2026-09-25T03:14Z: open, ready, mergeable, native squash auto-merge armed.
- Durable GitHub execution: CI run `36087272718`; desktop job `107921849474` remained `IN_PROGRESS` while Quote Hub, ingest, unit/typecheck, serial, tablet, mobile and CodeQL were green.
- Product boundary: point-in-time public 13F disclosures from the tracked manager roster; not total ownership, not a company rank and not a trading signal. Institutional stays in the separate Ownership family and is not promoted into v2 current-event truth.

First continuation action must reconcile `#734` from GitHub. If it merged, consume the merge SHA; if still running, do not duplicate or restart its checks.

### Held release slice — CompanyVisual

- PR: `#735 — feat(analysis): extract shared CompanyVisual fallback`
- Remote semantic head: `70aa69d4db07eb24814a816dc7b1c1e195f2cf7d`
- PR remains draft, based on the Ownership branch, with the last hosted run green.
- Studio has a clean local prepared integration commit `ceeda2c505b7346904d177b5be41b78ed1f77168`, parents `70aa69d4...` and `b2b51114...`.
- Important reconciliation fact: `70aa69d4..ceeda2c5` has **zero tree/blob delta**. `ceeda2c5` is ancestry-only preparation and must not be pushed merely to make the branch look current.

After `#734` is canonically merged, apply `RECONCILE_STATE` path/material-source review. If CompanyVisual-owned blobs and proof contracts remain unchanged, retarget `#735` to the accepted master and reuse the reviewed semantic head rather than pushing the ancestry-only commit. Only produce a new semantic head if current master materially changes the candidate behavior or proof contract.

CompanyVisual law:
- neutral deterministic fallback remains truthful;
- optional art may be same-origin or `data:image/...` only;
- external network artwork falls back safely;
- visual is decorative and cannot carry data/source/freshness/trade authority;
- do not publish the Paper NVIDIA/Blackwell mock artwork as sourced company truth.

### Built and held behind #735 — Event History + Topic Memory

- Branch: `sol/company-intelligence-event-history-20260925-sol-001`
- Exact pushed head: `22316bda90bd98a4872f3de9553bd180c208769d`
- No PR exists yet.
- Earlier full History head: `c0041e7b18b5fdb2c59217ea37bd9255cb4d583c`.

Implemented capability:
- compact Event History strip on Brief;
- full Event History & Comparison workflow;
- lawful v1 selectable event tape and structured metric matrix;
- v2 current-event block mechanically separated from read-only historical v1 context;
- Topic Memory summary and added/persistent/dropped lanes;
- first/last seen event/date and event count;
- explicit Historical v1 Context boundary in verified v2 mode;
- direct History ↔ Topic Memory workflow;
- EN/ZH and responsive desktop/tablet/mobile behavior.

Truth law:
- no second event/history/topic store, comparison engine, router or classifier;
- v1 rows/topics cannot overwrite, rank, label or explain the v2 current event;
- no topic importance score or inferred topic groups;
- keep `topicStatusLabel()` as the plain-label owner.

Latest recorded proof:
- Next route types + TypeScript PASS;
- evidence/plain-label/plain-language guards `95/95 PASS`;
- full Company Intelligence matrix `63 passed / 33 intentional skips / 0 failed`;
- Topic Memory workflow discriminator `47 / 21 / 0`;
- focused v1/v2 desktop/mobile visual proof `4/4 PASS`.

After `#735` is accepted, reconcile this branch against accepted master, open one bounded PR for the History + Topic Memory capability, and preserve its exact authority split.

### Production acceptance prover

- Branch: `sol/company-intelligence-live-proof-20260925-sol-001`
- Exact pushed head before this handoff commit: `e6ca8ab82a3fe909cc8dcfe06fa05c9e0e172d70`
- No PR exists yet; this is deliberate to avoid competing CI before the product chain is accepted.
- It contains the release-bound Company Intelligence prover plus the History/Topic Memory parent.

The prover binds all routes to one exact served 40-hex `data-dpl-id` and proves:
- anonymous auth boundary;
- signed-in desktop/mobile Brief, Results, Call + Q&A, Sources and Topic Memory;
- fixed Evidence overlay without canvas reflow;
- CompanyVisual and no horizontal overflow;
- chart → Analysis → Intelligence navigation;
- separate Institutional Ownership deep link;
- zero browser exceptions;
- redacted mode-0600 receipt.

Exit semantics:
- `0`: complete signed-in proof passed;
- `64`: malformed input or served-release mismatch;
- `70`: browser/product assertion failure;
- `78`: exact operator-authentication gate.

The signed-in prover accepts state only from git-ignored `terminal/e2e/.live-state/`. No valid operator state is present on this carrier. Do not commit, print or move authentication state outside that directory.

After the History + Topic Memory PR is accepted, refresh this branch so its diff is prover-only, open/merge it through normal review, then use it against the exact deployed release.

### Cumulative compatibility carrier

- Branch: `validation/research-paper-integration-20260924`
- Exact pushed head: `89dd6c7243693981c95c679e2dcfee9c5e2758d1`
- This is validation evidence only, never a release branch.
- It combines current Company Intelligence, History/Topic Memory, Overview, Financials, Earnings, Market and Ownership.
- Latest recorded proof: cross-program units `100/100`, Research Workspace navigation `21/6/0`, Company Intelligence `47/21/0`.

## Production state at checkpoint

Public route checked: `https://app.mastermind-x.com/analysis?symbol=NVDA&page=intelligence`

- HTTP: `200`
- Observed served `data-dpl-id`: `d71551154604981b5dc4468fbbe3dc7cae5ab9c6`
- Served release includes accepted Call + Q&A (`febda456...`).
- Served release does **not** include current master/Sources (`9aca42ee...`).
- Production was one protected-master commit behind at observation time.
- This is a read-only observation, not deployment acceptance or authenticated product proof.

Current Studio lacks the VPS root public key for the incumbent deployment command. When the final source chain is accepted, deployment requires the exact authorized Terminal release owner and current `DEPLOY.md` exact-target/preflight law. Never deploy a branch head or bypass the git-gated owner.

## Native Paper lane

Current procedural pin for the latest Paper pass is the same `605cd056...` Skillpack pin above.

Verified route — do not rediscover or replace:
- interpreter: `/Users/chriswong/.local/share/mastermind-paper/runtime/v3/venv/bin/python`
- guarded bridge: `/Users/chriswong/.local/share/mastermind-paper/runtime/v3/source/bridge.py`
- bridge SHA256: `26e3b5e8435d9a44f9476d29ae8d2f55819b9d7c05a0c008fb021da58d96e081`
- evidence directory: `/Volumes/Mastermind/evidence/terminal-company-intelligence-paper-touchup-20260925/`

Accepted native effects:
- desktop navigation/hero cleanup on preferred boards `B6L-1` / `BPV-1`;
- mobile sizing/readability changes on `AMI-1`;
- original Evidence drawer restored after an incomplete redesign attempt; current restored root is `UY9-0`;
- working indicators released for `B6L-1`, `BPV-1`, `AMI-1`.

No Paper effect remains `EFFECT_UNKNOWN`, but final visual correctness is incomplete:
- obtain native visual/dimension proof for mobile `AMI-1`;
- obtain native screenshot/readback for restored Evidence drawer `UY9-0` within `BPV-1`;
- only then continue Evidence refinement while preserving source truth.

Do not use obsolete drawer IDs `C05-1` or `UXX-0`, replay the refused content batch, create a new bridge, bypass the guarded route, or overwrite baseline boards.

## Evidence-lock and stylesheet ownership law

Protected Analysis evidence cryptographically locks shared `company-intelligence.css`. Company Intelligence page-owned rules must remain isolated:

- `company-intelligence-call.css`
- `company-intelligence-sources.css`
- `company-intelligence-ownership.css`
- `company-intelligence-visual.css`

Do not weaken the evidence lock or recapture unrelated screenshots merely to append page-specific CSS.

## Unresolveds and exact gates

1. `#734` desktop hosted shard and resulting auto-merge are running under GitHub; reconcile, do not retry.
2. `#735` needs post-#734 base reconciliation and protected release.
3. History + Topic Memory is built but has no PR and is held behind `#735`.
4. Production prover is built but has no PR and must become prover-only after its product parent merges.
5. Final accepted master still requires incumbent exact-target deployment.
6. Signed-in production proof requires a valid operator storage state in the existing git-ignored owner.
7. Paper mobile/Evidence visual readback remains incomplete due tool-layer safety refusals; no retry through another carrier as a bypass.

## Exact next actions in order

1. Read GitHub `#734`. If merged, record its merge SHA; if still running, preserve the existing auto-merge and advance only path-disjoint work.
2. Reconcile `#735` against accepted master using semantic-head/current-integration proof. Do not push ancestry-only `ceeda2c5`. Retarget to master, mark ready and arm protected merge only after compatibility is proven.
3. After `#735` merges, refresh and open the History + Topic Memory branch as one bounded PR; accept only on exact-head responsive/authority proof.
4. Refresh/open the live-prover branch as a prover-only PR after its product parent is accepted.
5. Deploy the final accepted `origin/master` through the incumbent exact-target release owner; read back preflight, target SHA, live marker and served `data-dpl-id`.
6. Run the authenticated release-bound prover. Acceptance requires the standalone Intelligence route, chart-hosted journey, Evidence overlay, exact Q&A→transcript handoff, Topic Memory, Ownership deep link and zero browser errors on the exact served release.
7. Independently, resume Paper only after reconciling the latest file writer/nodes; first prove `AMI-1` and `UY9-0`, then continue bounded Evidence refinement.

## DO NOT REDO / danger areas

- Do not rebuild accepted PRs `#725`, `#727`, `#728` or `#730`.
- Do not change Call + Q&A back to Transcript.
- Do not restore scroll-moving lens navigation.
- Do not create duplicate transcript, search, evidence, source, event history, topic memory, 13F or navigation owners.
- Do not invent topic labels/importance, consensus, price reaction or trade authority.
- Do not promote v1 ownership/history/topics into v2 current-event authority.
- Do not put page-owned CSS back into the evidence-locked shared stylesheet.
- Do not push ancestry-only local R6 commit `ceeda2c5` as a fake capability delta.
- Do not treat the integration branch, CI green, merge, deployment or anonymous route observation as production acceptance.
- Do not deploy through Vercel or from an unmerged branch.
- Do not expose operator storage state, credentials or private proof receipts.

## Resume instruction

Start with this command-level question, not a broad rescan:

> What is the current canonical state of PR #734 and protected Terminal master, and did any material source/path movement invalidate the reviewed CompanyVisual semantic head `70aa69d4...`?

Then execute the first safe dependency above. Preserve this mission until authenticated production acceptance or an exact human gate is the only remaining lane.
