# Comparison settings repair — current implementation and evidence

## Outcome and source
Continue the Chairman's Terminal chart quality and usability programme. Make the existing comparison-line controls readable, touch-friendly, keyboard-operable and faithful to the saved color/line settings, without replacing the chart or its preference owner.

Protected compatible Skillpack: Mastermind@0471cea4f891da1ec0c9fbeff10a9391f9cdd90f, v1.0.1. INDEX, COLD_START, ACTIVE_EXECUTION, WEB_CEO_DELEGATION, RECONCILE_STATE, REVIEW_RETURN and CLOSEOUT read at the same pin. Current Chairman continuation supplies intent. Native operation stays on Remote Desktop Commander, authorized Mac Studio, original claude/terminal-compare-settings-ux-20260922 branch/worktree. Base66f26abc5eb88880cbc8dc23851328ef73bb3353; later accepted options changes are disjoint from this component and its imports. Direct rationale: LOWER_TOTAL_OVERHEAD / PRINCIPAL_JUDGMENT. No overlapping comparison-settings writer found; no worker commissioned.

## Repair
Reuse chartSettingsUi's colorInputHex, numeric preview/commit bounds and native-dialog lifecycle. Keep the original palette and comparison callbacks. RGBA and short/eight-digit hex show their actual RGB; selecting a new color preserves the old opacity. Integer line widths are editable as drafts, valid values preview, and an empty draft restores the last value on commit rather than jumping to zero. Bounds disable the corresponding stepper.

A named native dialog now owns focus and prevents chart interaction behind it. Close is an actual button; line and scale choices expose selected state and use existing LEX translations. Tab/Shift+Tab wrap inside the visible enabled controls. Escape/Close/OK keep the existing live-save behavior; they are not represented as a cancel/revert operation. The scoped dark-token UI groups controls, displays a line appearance sample and uses44px targets on touch. No dependency, shared preference store, chart engine, signal mathematics or backend service is added.

The actual before-image is committed at terminal/docs/pr-crops/compare-settings-ux-20260922/desktop-before.png. It was captured by adding AAPL through the real comparison picker, selecting Percent, closing that picker and opening the actual comparison legend settings. Prior attempts that missed this chooser or left its scrim open were setup failures, not product defect proof.

## Discriminating qualification
The original component at bf1879ee has seven failures/one pass in eight comparison contracts. The candidate now passes all eight; all45 shared chartSettingsUi contracts also pass,53/53 combined. Opacity preservation remains covered. A jsdom attribute selector containing the literal plus sign returned null while direct enumeration showed that exact label and disabled=true. The assertion now checks exact DOM attribute equality AND disabled state; no expected state, label or behavior was relaxed. Real browser checks independently require the same bound button to be disabled.

The first actual dialog browser test found Shift+Tab escaped from the first control. That source defect was repaired with the dialog's bounded keyboard-cycle handler, and the full desktop EN/ZH journeys then passed: numeric drafts, saved line width/style/scale/color, hit-test reachability, Escape, reopen and persisted settings. Phone EN/ZH journeys also pass through Analysis hub → Compare → legend settings, including44px controls and no page errors/overflow.

The broad tablet setup initially tried the hidden watchlist. Source inspection confirms the existing tablet chrome hides the desktop comparison trigger and watchlist while the phone Analysis hub is not mounted at820px. That was the historical saved-layout-only proof boundary. The continuation below supersedes it with a real More → Compare creation action and empty-workspace tablet proof.

## Remaining gates
The preserved implementation has now passed fresh local qualification recorded below. A green required natural PR run on the new exact head/current protected base is still required before Ready; the prior hosted run failed the calendar-proof assertion and has not been waived. Production proof remains owed after protected merge and one exact-target git-gated deployment. Capability: BUILT_NOT_PROVEN; parent chart programme remains incomplete. #701/#705 remain accepted and DO_NOT_REDO; #713/#688 release work stays on its own carriers. Do not call any open PR, passing screenshot, queued run or dispatch-only check a production release.

## Tool/effect reconciliation
The earlier source rewrite refusal left the79-line original unchanged. This continuation observed the new native backend PID71803, performed directory/configuration preflight, reconciled the clean bf1879ee source, and subsequently wrote the repair through that same native carrier. No permission setting or alternate writer was used.

An intermediate unit-selector correction was also refused before a PID; readback confirmed no effect. Later real native source/test edits were accepted again. After exact target metadata/readback, the same-carrier correction was accepted and the focused test run passed53/53. This resolves that held edit; the old platform refusal is not a continuing source blocker. No blocked operation was moved to GitHub contents APIs or another actor. The first implementation checkpoint e68a5718195f298ef5905dc1e744a667631db987 was committed/pushed before later keyboard and test repairs; current publication must include those later effects.

## 2026-09-23 continuation: current proof, bounded CI repair

Current protected procedure pin: Mastermind `89582a372aa2a57ec500868ce6d79cd156219445`, Skillpack 1.0.1. The original comparison source and helper ownership remain unchanged from preserved head `1f3a357b2b034c9a359d46b440a7bdd3777adb1c`. The current Terminal protected base is `271bb55e2cc2e703169e17127874098c9670ab8e`; relevant shared movement was inspected, not assumed path-disjoint. Natural PR CI `35833232387` checked out integrated merge `dee87cb5589440f95ca56faa36e17b3f613328d1`. Unit/typecheck and tablet/mobile/serial shards passed; desktop had 334 passes, 55 skips and one failure repeated across all three attempts.

Artifact `10739693913` proves the two panes' calendar windows agree exactly, while their left logical indexes differ by 1.2843 and their right indexes differ by 48.7864. Different trading calendars can cross at one endpoint. The smallest repair makes the existing >5 discriminator compare both ends of the logical range, rather than only its left edge. Calendar tolerance (2.5 days), fixture data, gestures, timings, retries and product synchronization code are unchanged. Recorded-trace assertion replay passes the correct range and rejects copied whole ranges; it is explicitly not presented as a browser product-mutation test.

Fresh qualification: 53/53 component/shared-helper contracts; a separately started cold Chromium server with CI=1 and zero retries completed 20 browser tests (6 EN/ZH comparison journeys and 14 desktop/tablet pane-sync journeys), with seven intentional phone-grid skips and no failures. The six comparison screenshots were actually recaptured, visually reviewed and copied from this run. `terminal/docs/pr-crops/compare-settings-ux-20260922/EVIDENCE.json` binds their bytes, exact source files, report and log. The old before-image remains historical and untouched. Current proof files reside under `/Volumes/Mastermind/evidence/terminal-compare-settings-ux-20260922/qualification-20260923/`.

The successful local run does not waive fresh hosted integration checks. At that earlier boundary, tablet new-comparison entry and WebKit qualification were unfinished. The next section supersedes those two local-proof limitations; physical Safari and post-merge public comparison proof remain distinct obligations. Terminal's root explicitly declares a dark-only surface; no fake light-mode screenshot or new theme owner was introduced. No marker repair, accepted #701/#705 redesign, merged #713 branch, or Macro #7648 work was repeated.

## Tablet creation: current continuation proof

Current protected procedure is Mastermind `bf764f494b9cd0ecede6234bb472c3344c8e77cc`, v1.0.1/bootstrap1. The original #718 source carrier is retained. The new More → Compare entry uses the existing search-mode, seed and picker-open setters. It adds no state, service, preference parser or renderer. An explicit44px minimum height follows the failing34.125px hit-target measurement. #714 changes only a later TerminalShell rail-dock region; no marker, context-rail or chart-engine code was copied or modified.

The strengthened test first proves no comparison row exists, then physically taps More and Compare on820px tablet, adds AAPL through the existing picker, edits real overlay settings and reopens them to verify persistence. It no longer seeds a saved comparison layout or calls the layout API as a workaround. The old implementation failed because the entry was absent; the first repair failed the unchanged44px target requirement; the corrected source passes. All red evidence remains separate from the final captures.

Fresh final qualification: Chromium6/6 and WebKit6/6 EN/ZH desktop/tablet/mobile, zero retries and zero flaky results; full unit379files/6,089passes/4TODO; focused component/helper/boot/menu101passes; non-incremental TypeScript PASS. Browser-spec lint passes. TerminalShell has55 legacy lint errors and13 warnings outside the three added lines; no new changed-line finding is claimed as a whole-file lint pass. Six modal screenshots plus two tablet-entry screenshots were truly recaptured and visually reviewed. The updated EVIDENCE.json binds source, screenshots, reports and logs under the tablet-entry-20260923 proof directory.

Hosted natural PR CI35839197155 checked out953c0580a6cab9d139991178e15826ad9782b619 and failed ordinary marker-tap integration on both tablet and phone. Those checks remain failures; passing comparison proof does not waive them. Incumbent #688 correction comment5774793480 now records #718's additional impact. This comparison candidate stays draft with auto-merge off until current-base eligible proof passes. Study-manager native-touch entry and physical Safari acceptance remain outside this comparison repair; #713 desktop public WebKit proof is separately recorded in its receipt5792541602. No production deployment was performed.
