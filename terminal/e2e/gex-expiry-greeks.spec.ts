// Retired duplicate proof.
//
// Canonical expiry Vanna/Charm browser coverage now lives in:
// - exposure-expiry-greeks.spec.ts: transport -> Vanna -> expiry bars -> drawer -> Charm
// - responsive.spec.ts: enriched owner-native fixture across supported viewport widths
//
// This file previously duplicated that journey with a narrower route/selector setup and
// failed before reaching the Exposure controls. Keep this tombstone so older CI references
// resolve without re-introducing a second, divergent browser proof.
